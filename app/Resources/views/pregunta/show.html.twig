{% extends 'admin.html.twig' %}

{% block middle %}
    <header id="page-header">
        <h1>Preguntas</h1>
    </header>
    <div id="content" class="padding-20">
        <div class="panel panel-default">
            <div class="panel-heading">
                <strong>Preguntas externas</strong>
            </div><!-- end panel-heading -->
            <div class="panel-body">
                <table class="table table-bordered table-striped">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ preguntum.id }}</td>
                        </tr>
                        <tr>
                            <th>Nombre</th>
                            <td>{{ preguntum.nombre }}</td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td>
                                <a href="mailto:{{ preguntum.email }}">
                                {{ preguntum.email }}
                                </a>
                            </td>
                        </tr>
                        <tr>
                            <th>Pregunta</th>
                            <td>{{ preguntum.pregunta }}</td>
                        </tr>
                        <tr>
                            <th>Creación</th>
                            <td>{% if preguntum.createdAt %}{{ preguntum.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                        </tr>
                    </tbody>
                </table>
            </div><!-- end panel body-->
            <div class="panel-footer">
                <div class="btn-group" role="group">
                    <a class="btn btn-sm btn-warning" href="{{ path('pregunta_index') }}">Volver</a>
                    {#<a href="{{ path('pregunta_edit', { 'id': preguntum.id }) }}">Edit</a>
                    {{ form_start(delete_form) }}
                    <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}#}
                    </li>
                </div>
            </div><!--end panel footer -->
        </div><!-- end panel default -->
    </div><!-- end content -->
{% endblock %}
